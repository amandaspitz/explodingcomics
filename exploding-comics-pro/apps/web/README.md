# Web Workspace

This directory is reserved for the future Vue frontend.

Short-term approach:

- Keep the current production frontend untouched
- Build the backend first
- Add a compatibility layer so the current frontend can consume the new API when ready

Long-term approach:

- Replace the static site implementation with a Vue application backed by the Express API
